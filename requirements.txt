# NAVADA - Vercel Optimized Requirements
# Minimal dependencies for FastAPI deployment on Vercel

# Core FastAPI
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0

# Essential AI/LLM
openai==1.51.2
python-dotenv==1.0.1

# Minimal LangChain (core only)
langchain-core==0.2.38
langchain-openai==0.1.25
langsmith==0.1.129

# Essential data processing
pandas==2.1.4
numpy==1.26.2

# Basic utilities
requests==2.31.0
typing-extensions==4.9.0

# Remove heavy packages:
# - chainlit (not needed for API)
# - plotly/dash (visualization - too heavy)
# - langchain-community (has chromadb dependency)
# - jupyter/ipython (development only)
# - beautifulsoup4/lxml (web scraping - not essential)
# - reportlab (PDF generation - not essential)
# - matplotlib/seaborn/pillow (visualization - too heavy)